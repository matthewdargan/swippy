service: swippy-api
frameworkVersion: "3"

provider:
  name: aws
  runtime: provided.al2
  architecture: arm64
  stage: dev
  region: us-east-1

functions:
  ebay-find-by-category:
    handler: bin/ebay-find-by-category/bootstrap
    package:
      artifact: bin/ebay-find-by-category.zip
    events:
      - httpApi:
          path: /ebay-find-by-category
          method: get
    environment:
      STAGE: ${self:provider.stage}
    role: ebayFindFunctionRole

  ebay-find-by-keyword:
    handler: bin/ebay-find-by-keyword/bootstrap
    package:
      artifact: bin/ebay-find-by-keyword.zip
    events:
      - httpApi:
          path: /ebay-find-by-keyword
          method: get
    environment:
      STAGE: ${self:provider.stage}
    role: ebayFindFunctionRole

  ebay-find-advanced:
    handler: bin/ebay-find-advanced/bootstrap
    package:
      artifact: bin/ebay-find-advanced.zip
    events:
      - httpApi:
          path: /ebay-find-advanced
          method: get
    environment:
      STAGE: ${self:provider.stage}
    role: ebayFindFunctionRole

  ebay-find-by-product:
    handler: bin/ebay-find-by-product/bootstrap
    package:
      artifact: bin/ebay-find-by-product.zip
    events:
      - httpApi:
          path: /ebay-find-by-product
          method: get
    environment:
      STAGE: ${self:provider.stage}
    role: ebayFindFunctionRole

  ebay-find-in-ebay-stores:
    handler: bin/ebay-find-in-ebay-stores/bootstrap
    package:
      artifact: bin/ebay-find-in-ebay-stores.zip
    events:
      - httpApi:
          path: /ebay-find-in-ebay-stores
          method: get
    environment:
      STAGE: ${self:provider.stage}
    role: ebayFindFunctionRole

resources:
  Resources:
    ebayFindFunctionRole:
      Type: AWS::IAM::Role
      Properties:
        RoleName: "swippy-api-${self:provider.stage}-${self:provider.region}-ebay-find-function-role"
        AssumeRolePolicyDocument:
          Version: "2012-10-17"
          Statement:
            - Effect: Allow
              Principal:
                Service:
                  - lambda.amazonaws.com
              Action: sts:AssumeRole
        Policies:
          - PolicyName: "swippy-api-${self:provider.stage}-ebay-find-function-policy"
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: Allow
                  Action:
                    - logs:CreateLogGroup
                    - logs:CreateLogStream
                    - logs:PutLogEvents
                    - logs:TagResource
                  Resource: "*"
                - Effect: Allow
                  Action:
                    - ssm:GetParameter
                  Resource: "arn:aws:ssm:${self:provider.region}:*:parameter/${self:provider.stage}/ebay-app-id"

package:
  individually: true
  patterns:
    - "!./**"
    - ./bin/**
